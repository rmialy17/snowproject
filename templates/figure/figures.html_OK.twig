{% extends 'base.html.twig' %}

{% block title %}Tricks{% endblock %}


{% block header %}
    {% if not admin %}<header id="headerwrap" class="fullheight">
        <div class="container">
            <div class="intro-text">
                <div class="intro-lead-in wow bounceInDown" data-wow-delay="0.5s">We Can Help You <span class="wodry">Discover|Learn|Master</span></div>
                <div class="intro-heading wow bounceInUp" data-wow-delay="1s">DO<span class="thin">SnowTricks</span></div>
                <a href="{{path('figures')}}#lestricks" class="page-scroll btn btn-xl btn-negative  wow bounceInDown" data-wow-delay="2s">Tell Me More <i class="fa fa-angle-down fa-2x"></i></a>
            </div>
        </div>
    </header>{% endif %}
    
{% endblock %}


{% block body %}
{% for message in app.flashes('success') %}
    <div class="alert alert-success">
        {{message}}
    </div>
{% endfor %}


{# <div class="row no-gutters">
    {% for figure in figures %}
        <div class="col-12 col-lg-6 p-1">
            <div class="card mb-3">
                <div class="row no-gutters align-items-center p-2">
                    <div class="col-12 col-md-4">
                        <img src="{{asset('images/' ~ figure.image)}}" class="card-img">
                    </div>
                    <div class="col">
                        <div class="card-body">
                            <h5 class="card-title">{{voiture.immatriculation}}</h5>
                            <p class="card-text">
                                <div>Marque : {{voiture.modele.marque.libelle}}</div>
                                <div>Modele : {{voiture.modele.libelle}} - PM : {{voiture.modele.prixMoyen}}</div>
                                <div>Nombre de porte : {{voiture.nbPortes}}</div>
                                <div>Année : {{voiture.annee}}</div>
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
 {% endfor %}
</div> #}

{% block monTitre %}Les Tricks{% endblock %}    
{# display navigation #}

        {% if admin %}
        <a href="{{path('creationFigure')}}"><button class=" btn btn-success">Ajouter une figure</button></a>
        {% endif %} 
        <div class="container">
            <div class="row">
                {% for figure in figures %}
                <!-- <div class=" col-md-4"> -->
                    <div class="col-sm-4 py-2">
                        <div class="card h-100 text-white bg-dark">
                            <div class="card-body"> 
                    <!-- <div class="card" style="width: 18rem;"> -->
                        <a href="{{path('afficher_figure',{'id' : figure.id})}}"> <img src="{{ asset('/images/' ~ figure.imagetop) }}" class="card-img-top" alt="Image" width="150" height="200"></a>
                      <div class="card-body">
                        <h5 class="card-title">{{figure.nom}}</h5>
                        <!-- <a href="#" class="btn mr-2"><i class="fas fa-link"></i> Visit Site</a> -->
                        <!-- <a href="#" class="btn"><i class="fab fa-github"></i> Github</a> -->
                        {% if app.user %}
        
                            <a href="{{path('modifFigure',{'id' : figure.id})}}"><i class="fa fa-pencil" aria-hidden="true"></i></a>
                        {% endif %}   
                        {% if is_granted ('ROLE_ADMIN') or figure.user == app.user %}
                            <form method="post" class="col" action="{{path('supFigure',{'id':figure.id})}}" onsubmit="return confirm('Voulez-vous vraiment supprimer ?');">
                                <input type="hidden" name="_method" value="SUP">
                                <input type="hidden" name="_token" value="{{csrf_token('SUP' ~ figure.id)}}">
                                <button style="border:transparent;background:transparent;" type="submit"  value="supprimer"><i class="fa fa-trash-o" aria-hidden="true"></i></button>
                            </form>
                        {% endif %}  
                        </div>
                    </div>
                </div>
            </div>   
        {% endfor %}
        </div>
        <div class="navigation">
            {{ knp_pagination_render(figures) }}
        </div>
 
{% endblock %}
